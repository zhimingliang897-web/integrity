api:
  # 安全：不要把真实 Key 写进仓库。请使用环境变量（推荐）或本地 _secrets 版本覆盖。
  api_key: sk-0ef56d1b3ba54a188ce28a46c54e2a24
  base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: qwen-plus
  provider: dashscope
defaults:
  category: 随机
  phrase_count: 5p
  style: 轻松幽默
image:
  format: png
  height: 1440
  output_dir: output
  width: 1080
obsidian:
  attachments_folder: attachments
  queue_folder: 01_发布队列
  vault_path: E:/Obsidian/小红书运营
xiaohongshu:
  # 安全：Cookie 属于隐私信息，不要写进仓库。请改用 `cookies.json`（见 README）或从 _secrets 恢复。
  cookie: ""
