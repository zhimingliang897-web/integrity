# organize_config.yaml — organize.py 全局 API 配置
# 存放位置：e:/integrity/tools/（与 organize.py 同目录）
# 一次配置，对所有目录整理均生效

api:
  provider: "dashscope"
  # 安全：不要把真实 Key 写进仓库。请用环境变量 DASHSCOPE_API_KEY 或改为本地私有配置覆盖。
  api_key: "sk-your-key-here"
  model: "kimi-k2.5"
